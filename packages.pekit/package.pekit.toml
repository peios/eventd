# Base package metadata for eventd.

format = "peipkg"

[package]
# Revision 3 authenticates metric publishers and makes the log socket a
# peinit-only broker endpoint.
version      = "{{version}}-3"
architecture = "x86_64"
license      = "MIT"
