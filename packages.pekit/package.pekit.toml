# Base package metadata for eventd.

format = "peipkg"

[package]
# Revision 2 adds the evctl client to the package payload.
version      = "{{version}}-2"
architecture = "x86_64"
license      = "MIT"
