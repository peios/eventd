# Base package metadata for eventd.

format = "peipkg"

[package]
# Revision 8 exposes histogram percentile overflow as an explicit result.
version      = "{{version}}-8"
architecture = "x86_64"
license      = "MIT"
